package com.example.ragapp;

import org.junit.jupiter.api.Test;

/**
 * 기본 테스트.
 * 참고: @SpringBootTest 로 전체 컨텍스트를 띄우면 Ollama 연결이 필요하므로,
 * 학습용 단계에서는 비워 둡니다. (앱 실행은 ./mvnw spring-boot:run 으로 합니다)
 */
class RagAppApplicationTests {

    @Test
    void contextLoads() {
    }
}
